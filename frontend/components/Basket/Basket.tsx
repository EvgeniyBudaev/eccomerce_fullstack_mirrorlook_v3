import React, { useEffect } from "react";
import { useDispatch } from "react-redux";
import styles from "./Basket.module.scss";

export const Basket: React.FC = () => {
  return (
    <div className={styles.Basket}>Basket Page</div>
  );
};
