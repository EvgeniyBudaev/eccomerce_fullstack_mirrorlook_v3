export enum ROUTES {
  CART = "/cart/",
  CONFIRM_REGISTRATION = "/confirm-registration",
  HOME = "/",
  LOGIN = "/login",
  MIRRORS = "/mirrors/",
  ORDER = "/order",
  RECIPIENT = "/recipient",
  REVIEWS = "/reviews/",
  SHIPPING = "/shipping",
  SIGNUP = "/signup",
}
