export enum ROUTES {
  CART = "/cart/",
  CONFIRM_REGISTRATION = "/confirm-registration",
  CONFIRM_RESET_PASSWORD = "/confirm-reset-password",
  HOME = "/",
  LOGIN = "/login",
  MIRRORS = "/mirrors/",
  ORDER = "/order",
  RECIPIENT = "/recipient",
  REVIEWS = "/reviews/",
  SHIPPING = "/shipping",
  SIGNUP = "/signup",
  THANKS = "/thanks",
}
