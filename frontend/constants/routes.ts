export enum ROUTES {
  CART = "/cart/",
  HOME = "/",
  LOGIN = "/login",
  MIRRORS = "/mirrors/",
  ORDER = "/order",
  RECIPIENT = "/recipient",
  REVIEWS = "/reviews/",
  SHIPPING = "/shipping",
  SIGNUP = "/signup",
}
